{
  "name": "hs-example-plugin",
  "version": "1.0.0",
  "description": "Home Screens plugin template — clone this to build your own plugin",
  "license": "MIT",
  "scripts": {
    "dev": "vite build && (vite build --watch & npx -y serve@latest . -l 5173 --cors --no-clipboard)",
    "build": "vite build"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
